/*

if (true) {
  let myValue = 20;
}

console.log(myValue);

*/

/*

The above code will raise an error (ReferenceError). As myValue is defined 
within the if block, its scope is within it, and thus the console.log method
can't access the variable.

*/