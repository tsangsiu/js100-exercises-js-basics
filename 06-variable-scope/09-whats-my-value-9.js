/*

const a = 1;

function myFunction() {
  a = 2;
}

myFunction(a);

*/

/*

The above code will raise an error. It is because the variable a is declared as
a constant, it cannot be changed in myFunction().

And, although myFunction() accepts no argument, passing any arguemnts in the 
function would simply be ignored.

*/